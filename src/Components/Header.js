import React from "react";

const Header = () => (
  <div>
    <header className="App-header">
      <h1 className="App-title">React TodoList</h1>
    </header>
    <p className="App-intro">TodoList by react --ddcjddb</p>
  </div>
);

export default Header;
